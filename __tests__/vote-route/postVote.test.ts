describe('Teste Temporário', () => {
    test('Pronto para operar e aguardando mudanças no código', () => {
      const result: Number = 2;
  
      expect(result).toBeGreaterThan(1);
  
      console.log('Esse é um teste temporário e sofrerá correção assim que o código for implementado.');
    });
  });
